/// <reference path="../../typings/index.d.ts" />
describe('test', () => {
  it('should work', () => {
    expect(true).toBe(true);
  });
});

describe('testt', () => {
  it('should work', () => {
    expect(true).toBe(true);
  });
});

describe('test', () => {
  it('should work', () => {
    expect(true).toBe(true);
  });
});

